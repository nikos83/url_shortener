<p><%= t('devise.mailer.greeting', email: @resource.email) %></p>

<p><%= t('devise.mailer.unlock_instructions.account_locked') %></p>

<p><%= t('devise.mailer.unlock_instructions.click_to_unlock') %></p>

<p><%= link_to t('devise.mailer.unlock_instructions.action'), unlock_url(@resource, unlock_token: @token) %></p>
