<p><%= t('devise.mailer.greeting', email: @resource.email) %></p>

<p><%= t('devise.mailer.reset_password_instructions.instructions') %></p>

<p><%= link_to t('devise.mailer.reset_password_instructions.action'), edit_password_url(@resource, reset_password_token: @token) %></p>

<p><%= t('devise.mailer.reset_password_instructions.ignore') %></p>
<p><%= t('devise.mailer.reset_password_instructions.note') %></p>
